class Gameboard {
    placeShip(coord) {

    }
    receiveAttack(coord) {
        /*
        if the coordinate contains a ship then it that ship will get hit
        which ship is located at this coordinate ? how will we know about that ? 
        missed shot also needs to stored
         */
    }
    missedAttacks() {

    }
    areAllShipsSunk() {
        
    }
}

export { Gameboard }; 

/**
well, what will gameboard contain ? is it like a grid ? but I haven't created one
so, how do I do all these methods ? based on heuristics ? 

 */